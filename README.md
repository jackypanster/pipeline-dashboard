# pipeline-dashboard
Read-only state-machine dashboard for the jackypanster/pipeline skill-aggregation pipeline — renders a target repo's .pipeline/ git+md state bus (current.json + cards) as a static board.html.
