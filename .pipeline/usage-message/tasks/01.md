---
id: "01"
title: Usage line on argument errors
status: todo
attempts: 0
spec-rev: c6a9041
verify: npm run build && npm test -- usage
spec-paths: test/usage.spec.test.ts
impl-paths: src/cli.ts
---
# Usage line on argument errors

Per arch.md: add `const USAGE = "Usage: pipeline-dashboard <target-repo> [--out <board.html>]";`
and `function usageError(): number { console.error(USAGE); return 1; }` in src/cli.ts; the three
ARGUMENT-error `return 1` sites in run() (no target / unknown arg / --out missing value) become
`return usageError()`. The isInsidePipelineDir guard and the catch path stay EXACTLY as today.
stderr only; exit codes unchanged; valid runs byte-identical.

## Freeze coverage
All three behaviors frozen in test/usage.spec.test.ts (2 red + 1 green pin). Nothing manual.
